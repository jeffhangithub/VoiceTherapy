# VoiceTherapy · 响应延迟优化（分析 + 实施路径）

> 状态：方案 · 分支：`feat/latency-opt` · 日期：2026-09-02
> 范围：以**当前网页(WebRTC)实现**优先落地；**共性方案**同时并入 2.0 原生 App 设计。
> 目标：把「咨询者讲完一句 → AI 咨询师首声回应」从 **~4–5s 降到 ~2–2.5s**，激进可达 ~1.5s。

---

## 1. 当前延时实测分解（据 runner 日志）

一段干净回合的时间戳（秒）：

```
用户停止 ──ASR──▶ 转写结果 ──LLM──▶ TTS收到文字 ──TTS──▶ Bot started speaking
 X        +0.9~2.9      +0.75~2.3        +1.1~1.4
```

| 阶段 | 实测 | 主要成因 |
|---|---|---|
| **ASR** | +0.9 ~ 2.9s（最不稳） | faster-whisper 说话结束后**整段**才转写；转写耗时全在停止后发生 |
| **LLM** | +0.75 ~ 2.3s | Hermes 完整 agent，带 skills/vault/工具上下文，首 token 慢 |
| **TTS** | +1.1 ~ 1.4s | edge 等整句/首 chunk 才出音频 |
| **端到端** | **~4–5s**（干净单回合估） | 三段近似串联 |

> 注：trace 含回声污染（转写为残词），绝对值有噪声；阶段占比为可靠参考。

---

## 2. 优先级路径（先按网页实现）

### P1 — LLM 首 token 提速（B）【共性→2.0】
**网页做法**：
1. 缩短 voice 通道的 system/developer 指令（只保留咨询人设 + 必要记忆钩子，砍掉用不到的工具描述/大段背景）。
2. 指令显式要求「**第一句给短回应**（如"我在听""嗯"）再补充」→ 让首声早到。
3. 确保 LLM **token 流式**且 TTS **收到首句即播**（不缓存等整段）。
4. 会话若用完整 Hermes agent，可对 voice 回合限制 `max_tokens`/首段长度，令首 chunk 更快。
**预期**：省 ~0.5–1s。
**验收**：日志中 `run_tts 收到文字` 相对 `转写结果` 间隔 < 1s。

### P2 — TTS 首句即播（C）【共性→2.0】
**网页做法**：
1. 确认 EdgeTTSService 拿到 LLM 第一句**立刻合成、立刻 push**，不等后续句子。
2. 检查首 chunk 合成耗时；edge 换更近/更快端点或降低单句字数。
**预期**：省 ~0.5s。
**验收**：`run_tts 收到文字 → 合成 PCM` 间隔 < 0.6s，且首字更早出声。

### P3 — 流式 ASR / 边说边转（A，杠杆最大）【共性→2.0】
**网页做法**（ASR 引擎在 Mini 侧，客户端无关）：
1. 把「VAD 停后才整段转写」换成 **流式 ASR**：Silero VAD 开口即把音频分片喂 SenseVoice/FunASR 流式转写，边说边出中间结果。
2. 用户停时文字已基本就绪 → **ASR 从关键路径拿掉**。
3. 附带价值：出中间结果 → 网页实时显示（#3 UX 顺带升级）。
**预期**：省 ~1–2.5s（最大单项）。
**验收**：`用户停止 → 最终转写` < 0.5s；说话中已能看到部分文字。
> 属较大改造（换 ASR 引擎），作为 P3 放后；先做 P1/P2 快赢。

### P4 — 网络 / 同网（D）
**网页做法**：手机尽量连 **同 WiFi**，上下行单向由 ~0.65s(蜂窝/DERP) 降 <0.05s。
**预期**：端到端省 ~1s（纯运营，零代码）。
**验收**：Mini↔手机 ping < 50ms 时测得端到端缩短。

---

## 3. 端到端目标

| 阶段 | 现状 | 目标(组合后) |
|---|---|---|
| 感知首声 | ~4–5s | **~2–2.5s**；激进(流式ASR+TTS+轻persona+同WiFi) **~1.5s** |

实施节奏：P1 → P2（快赢）→ P4（运营）→ P3（大改）。

---

## 4. 里程碑 / 验收

- **M1**（P1+P2）：LLM 首段 <1s、TTS 首 chunk <0.6s；首声 ~3s。
- **M2**（P1+P2+P4）：同 WiFi 下首声 ~2.5s。
- **M3**（+P3 流式 ASR）：首声 ~2s 内，且说话中见中间转写。

每步 headless/日志可验项先过，真机(手机+真实网络)后置，记录实测前后对比。

---

## 5. 与 2.0 原生 App 的共性方案表

| 方案 | 1.x 网页 | 2.0 原生 | 备注 |
|---|---|---|---|
| LLM 轻语音 persona + 短首句 | P1 | 沿用 | 传输无关，原生照搬 |
| TTS 首句即播 | P2 | 沿用 | 传输无关 |
| 流式 ASR | P3(Mini 侧) | 原生更顺(端上流式) | 原生可在手机本地流式，出中间转写+隐私 |
| 降低下行 RTT | 同 WiFi | 同 WiFi/自建 | 原生也受网络影响 |
| 首字出声(前瞻 TTS) | 可选 | 原生易做 | 原生音频栈更灵活 |

**同一原则**：延迟优化的大头（ASR 位置、LLM 首 token、TTS 首 chunk）与"浏览器 or 原生"无关，属**共性**；2.0 原生因 native 音频栈，TTS/ASR 微调更自由。
